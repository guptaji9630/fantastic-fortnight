apiVersion: v1
kind: Service
metadata:
  name: golang-logging-service
  labels:
    app: golang-logging-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: golang-logging-service